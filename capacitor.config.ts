import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sharksvsdolphins.app',
  appName: 'SharksVsDolphins',
  webDir: 'dist'
};

export default config;
