import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sharksvsdolphins.app',
  appName: 'Sharks vs Dolphins',
  webDir: 'dist'
};

export default config;
