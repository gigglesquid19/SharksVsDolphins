/** Size of the square simulation grid (world-space units, not pixels). */
export const SIZE = 100;

/** Size of the rendered canvas in pixels; world-space is scaled to this. */
export const CANVAS_SIZE = 600;
